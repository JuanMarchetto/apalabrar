{
  "name": "@apalabrar/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Apalabrar shared UI components (Solid + kobalte + Tailwind).",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "@kobalte/core": "^0.13.7",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "@solidjs/testing-library": "^0.8.10",
    "@testing-library/jest-dom": "^6.5.0",
    "@vitest/coverage-v8": "^3.0.0",
    "happy-dom": "^15.7.0",
    "typescript": "^5.6.0",
    "vite-plugin-solid": "^2.10.2",
    "vitest": "^4.1.5"
  }
}
