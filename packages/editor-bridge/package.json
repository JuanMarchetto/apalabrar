{
  "name": "@apalabrar/editor-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript bindings for the apalabrar-editor-core WASM module.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "apalabrar-editor-core": "file:../../crates/editor-core/pkg"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "fast-check": "^3.22.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5"
  }
}
