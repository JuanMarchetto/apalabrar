# @apalabrar/server

Placeholder. The sync + auth server is implemented in Rust (Axum + sqlx + Postgres) and lands in **v1** — when real-time collab and cloud sync ship.

For v0, the editor is fully client-side: OPFS for storage, no backend, no login.
