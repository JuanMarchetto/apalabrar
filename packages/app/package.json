{
  "name": "@apalabrar/app",
  "version": "0.0.0",
  "private": true,
  "description": "Apalabrar — browser app shell (Solid + Vite).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "coverage:check": "vitest run --coverage --coverage.thresholds.lines=85 --coverage.thresholds.branches=75",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "size-limit": "size-limit",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "size-limit": [
    {
      "name": "JS bundle (gzipped) — Validation Gate 2 cap 200 KB",
      "path": "dist/assets/*.js",
      "limit": "200 KB"
    },
    {
      "name": "WASM core (gzipped) — Validation Gate 2 cap 4 MB target / 10 MB hard ceiling",
      "path": "dist/assets/*.wasm",
      "limit": "4 MB"
    }
  ],
  "dependencies": {
    "@apalabrar/editor-bridge": "workspace:*",
    "@apalabrar/ui": "workspace:*",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "@solidjs/testing-library": "^0.8.10",
    "@testing-library/jest-dom": "^6.5.0",
    "@vitest/coverage-v8": "^3.0.0",
    "fast-check": "^3.22.0",
    "happy-dom": "^15.7.0",
    "tailwindcss": "^4.0.0-beta.4",
    "@tailwindcss/postcss": "^4.0.0-beta.4",
    "postcss": "^8.4.47",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.10.2",
    "vitest": "^3.0.0"
  }
}
