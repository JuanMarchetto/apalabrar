# cargo-deny configuration
# See https://embarkstudios.github.io/cargo-deny/

[advisories]
version = 2
ignore = [
    # yaml-rust is unmaintained but has no safe upgrade path. Pulled in
    # transitively via comrak -> syntect. Re-evaluate when syntect drops it
    # or when we replace comrak.
    "RUSTSEC-2024-0320",
    # atomic-polyfill is unmaintained (author deprecated it in favor of
    # portable-atomic). Transitive via heapless -> postcard -> loro. No
    # action available until heapless updates.
    "RUSTSEC-2023-0089",
    # bincode 1.x is unmaintained but the maintainers consider 1.3.3 a
    # complete release; no security issue. Transitive via syntect.
    "RUSTSEC-2025-0141",
]

[licenses]
version = 2
confidence-threshold = 0.8
# Permissive licenses commonly seen in the Rust ecosystem. Apalabrar's own
# code is MIT (see Cargo.toml). Transitive deps under any of these are
# acceptable; AGPL/GPL/LGPL remain forbidden by omission.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "0BSD",
    "Unicode-DFS-2016",
    "Unicode-3.0",
    "CC0-1.0",
    "Zlib",
    "MPL-2.0",
    # Boost Software License — permissive, OSI-approved. Used by xxhash-rust.
    "BSL-1.0",
]

[bans]
# The workspace pulls in duplicate transitive versions via Loro and others;
# warn but don't block. Enforce strict deduplication once 1.0 lands.
multiple-versions = "warn"
wildcards = "deny"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
